type NewsItem {
  title: String!
  link: String!
  author: String
  pubDate: String!
  content: String
}

type Page {
  title: String
  image: String
}

type Query {
  me: User
  news: [NewsItem]
  pages: [Page]
}

type User {
  id: ID!
  email: String
}
